streamlit==1.61.1
pandas>=2.2.0
pypdf>=6.0.0
cryptography>=45.0.0
PyMuPDF>=1.26.0
Pillow>=12.2.0
pillow-heif>=1.5.0
zstandard>=0.23.0
# Full redeploy marker: load atomic sample-memory persistence (2026-08-02).
# Literature package cache refresh marker (2026-08-16).
# Full-library deep retrieval redeploy marker (2026-08-19).
# Resilient background answer persistence redeploy marker (2026-08-19).
# DeepSeek final-answer fallback redeploy marker (2026-08-19).
# Concise answer and sidebar full-redeploy marker (2026-08-20).
# Restore complete answers while keeping only deduplication and source cleanup (2026-08-20).
# Evidence, workflow and privacy full-redeploy marker (2026-08-23).
